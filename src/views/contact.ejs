<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
        <title><%= title %>
            - Examen</title>
        <%- include('partials/head'); %>
    </head>
    <body>
        <%- include('partials/navigation'); %>

        <div class="jumbotron image-banner contact-banner">
            <div class="container">
                <h1 class="display-4">Contacto</h1>
                <p class="lead">Lorem ipsum dolor sit amet consectetur adipisicing elit. Et voluptatum reiciendis, modi sint odit dolorum aliquid voluptatibus facilis saepe qui necessitatibus explicabo nulla doloremque ipsum eligendi? Assumenda itaque voluptatibus minima.</p>
            </div>
        </div>

        <div class="container mt-5 mb-4">

            <% if (success != 'undefined') { %>
                <% if (success == 1) { %>
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <strong>Enviado
                        </strong>
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                <% } %>
                <% if (success == 2) { %>
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <strong>No se pudo enviar
                        </strong>
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                <% } %>
            <% } %>
            <div class="row">
                <div class="col-md-5 offset-md-4">
                    <div class="card">
                        <div class="card-header bg-dark text-white text-center">
                            <h3>Contact Us</h3>
                        </div>
                        <div class="card-body">
                            <form action="/enviar-email" method="POST">
                                <div class="form-group">
                                    <input type="email" id="remitente" name="remitente" class="form-control" placeholder="Remitente(email)">
                                </div>
                                <div class="form-group">
                                    <input type="text" id="asunto" name="asunto" class="form-control" placeholder="Asunto">
                                </div>
                                <div class="form-group">
                                    <textarea class="form-control" id="mensaje" name="mensaje" name="" id="" cols="30" rows="10" placeholder="Mensaje"></textarea>
                                </div>
                                <div class="form-group">
                                    <button class="btn btn-primary btn-block">Enviar</button>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <%- include('partials/scripts'); %>
    </body>
</html>
